{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^prebuild", "prebuild"],
            "outputs": ["output-file.txt", "dist/**"]
        },
        "prebuild": {},
        "lint": {},
        "type-check": {}
    }
}
